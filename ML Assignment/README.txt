# This is a submission for the problem statement given on https://s3-ap-southeast-1.amazonaws.com/aimonk/SDE1-problem+statement.pdf

# The code has been written in python and does not require compilation. However, to run the program please ensure you have the following
  installed on your system
  - Python 3.x
  - pip
  - Pandas Library 
	- Install Command : pip install pandas 
  - Numpy Library
	- Install Command : pip install numpy

# To run the script, please ensure the data file 'points.txt' is in the same directory as the script or you will have to give the full
  path to the data file.

# Run the script by typing the following command in the directory of script : ./prog points.txt 2

# The output will be generated and stored in 'clusters.txt' which will be created in the same directory as the script.